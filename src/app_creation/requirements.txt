# Versions épinglées (CDC) : une montée de version ne peut pas casser l'app.
# streamlit >= 1.41 requis : sélection de lignes/cellules dans st.dataframe
# (ruban activé au clic sur une ligne) et boîtes de dialogue larges.
streamlit==1.49.1
# >= 0.81.0 requis : module w.postgres (génération du jeton Lakebase).
databricks-sdk==0.81.0
# Client Postgres (Lakebase) — wheel binaire, pas de compilation au déploiement.
psycopg[binary]==3.2.3
pandas==2.2.3
numpy==1.26.4
opencv-python-headless==4.10.0.84
# Décodage des photos HEIC/HEIF (format par défaut iPhone / Android récents).
pillow==10.4.0
pillow-heif==0.18.0
